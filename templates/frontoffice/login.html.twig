{% extends "/frontoffice/layout.html.twig" %}

{% block head %}
<title>Se connecter</title>
{% endblock %}

{% block content %}

    <div class="contentLogin">

            <div id="loginBanniere">
            <div class="banniere">
                <h4>Se connecter</h4>
            </div>
        </div>

    
        <!-- Afficher le formulaire de saisie du mot de passe -->
        <h3 class="titleConnection">Veuillez saisir votre identifiant et le mot de passe pour vous connecter</h3>

        <form method="POST">
            <div class="form-group">
                <label for="">Pseudo ou email</label>
                <input type="text" name="username" class="form-control" placeholder="Utilisateur" />
            </div>

            <div class="form-group">
                <label for="">Mot de passe<a href="{{ path('forgetpassword') }}"> (J'ai oublié mon mot de passe)</a></label>
                <input type="password" name="password" class="form-control" placeholder="Mot de passe" />
            </div>

            <button class="btn btn-primary">Se connecter</button>

        </form>

        <h6 class="titleinscription">Pas encore inscrit ?
        <a class="btn btn-link" href="{{ path('register') }}">S'inscrire</a></h6> 
    </div>

{% endblock %} 